1. Class diagrams display the the relation between a class and it's objects.
The objet diagram displays a  list of objects in red boxes.
2. Whenever you change the code (adding or removing classes)
3. An object diagram changes when you create or delete objects.
4. "tutor" 
NumberDisplay Hours = new NumberDisplay(24);
Hours.increment();
6. Call the increment method 60 times in the 'minutes' object
then increment the 'hour'by 1. Then check if the value in 'minutes' is "0"
10. Error: non-static method getValue() cannot be referenced from a static context.
11. Error: '.class' expected
13. replacementValue
14.  
15. 
! false
(2 > 2) ((4 == 4) && (1 < 0)) ││
(2 > 2) (4 == 4) && (1 < 0)
16. 
17. 
18. 
19. There's no difference beteween the writings, I just receive an error.
20.  
   9 + 3 "cat"
  "12cat" 

21. It's "00:00" because no time was set in "setTime".
22. 





